---
name: branch-workflow
description: Branching and commit rules for this repo. Use BEFORE the first edit of any task, and whenever creating/switching a git branch, starting a plan iteration, or being asked to commit/push. Covers the current-branch check, the `custom-yyyy-MM-dd` base branch rule, the `c/<kebab-case>` working-branch naming, and the do-not-commit rule.
---

Read `docs/agent/branching.md` and follow it.

Claude Code's vendor prefix is `c/`.
