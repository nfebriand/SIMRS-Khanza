# Builds the application without compiling Jasper report designs.
# The resulting artifact contains no reports and will fail on any report action.

name: Java CI

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      targets: jar
      artifact-name: Package
