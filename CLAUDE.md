@AGENTS.md

# Claude specific instructions

#### Workflow guideline
- Branch prefix: use `c/` in place of the vendor-letter prefix described in [`docs/agent/branching.md`](docs/agent/branching.md). Every other rule in that document applies unchanged.
